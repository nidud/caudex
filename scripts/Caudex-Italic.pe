#!/usr/bin/env fontforge

if ( $argc == 2 )
    version = $1
else
    version = "2.0"
endif
Open("../fonts/src/Caudex-Italic.sfd", 1)
SetFontNames("", "", "", "", "", version)
Generate("../fonts/Caudex-Italic.woff2")
Generate("../fonts/Caudex-Italic.ttf" )
Close()
Quit()
