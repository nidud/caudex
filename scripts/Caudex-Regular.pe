#!/usr/bin/env fontforge

if ( $argc == 2 )
    version = $1
else
    version = "2.0"
endif
Open("../fonts/src/Caudex-Regular.sfd", 1)
SetFontNames("", "", "", "", "", version)
Generate("../fonts/Caudex-Regular.woff2")
Generate("../fonts/Caudex-Regular.ttf" )
Close()
Quit()
